# Clear all the cache of panel resources 

The panel resources files will be stored in the current app storage directory. you can call this method to clean up.

**Sample code**

``` java
TuyaPanelSDK.getPanelInstance().clearPanelCache();
```